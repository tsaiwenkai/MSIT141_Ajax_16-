
<div class="col-6">
    <form name="useInfo" method="post" enctype="application/x-www-form-urlencoded" >
        <div class="form-group">
            <label for="InputName">姓名</label>
            <input type="text" class="form-control" id="InputName" name="Name">
            <small id="nameHelp" class="form-text text-muted"></small>
        </div>
        <div class="form-group">
            <label for="InputEmail">電子郵件</label>
            <input type="text" class="form-control" id="InputEmail" name="Email">
        </div>
        <div class="form-group">
            <label for="InputAge">年紀</label>
            <input type="number" class="form-control" id="InputAge" name="Age">
        </div>
        <div class="custom-file  mt-3">
            <input type="file" class="custom-file-input" id="customFile" name="file">
            <label class="custom-file-label" for="customFile">選擇檔案</label>
        </div>
        <button type="submit" class="btn btn-primary mt-3">Submit</button>
    </form>
    <div class="alert alert-info" id="div1"></div>
</div>
<div class="col-3"></div>
@section Scripts{
    <script>
    const btnSubmit = document.querySelector('.btn');
         const divInfo = document.querySelector('#div1');

        //const nametext = document.querySelector(`#InputName`)
        //const smalltext = document.querySelector(`#nameHelp`)

         @*btnSubmit.addEventListener('click', (evt) => {
        evt.preventDefault(); //停止預設行為 submit link
        const formData = new FormData(document.useInfo);
        //透過 fetch() 上傳圖檔
        fetch('@Url.Content("~/Api/Register")', {
            method: 'POST',
            body: formData
        })
            .then(response => response.text())
            .then(data => divInfo.innerHTML = data);*@


             btnSubmit.addEventListener("click", (evt) => {
            evt.preventDefault();//取消預設事件ex:btnSubmit更新頁面
            let formData = new FormData(document.useInfo);
            fetch('@Url.Content("~/Api/Register")', {
                method:"POST",
                body: formData
            }).then(r =>r.text()).then(d => divInfo.innerHTML = d);
            @*
            const xhr = new XMLHttpRequest();

            xhr.addEventListener("load", () => {
                const data = xhr.responseText;
                divInfo.innerHTML = `${data}`;
            });
            xhr.open(`POST`, `@Url.Content("~/api/Register")`);
            xhr.send(formData);*@
        });


         @*btnSubmit.addEventListener("click", (evt) => {
            evt.preventDefault();//取消預設事件ex:btnSubmit更新頁面

            let formData = new FormData(document.useInfo);
            const xhr = new XMLHttpRequest();

            xhr.addEventListener("load", () => {
                const data = xhr.responseText;
                divInfo.innerHTML = `${data}`;
            });
            xhr.open(`POST`, `@Url.Content("~/api/Register")`);
            xhr.send(formData);
        });*@


    </script>
}